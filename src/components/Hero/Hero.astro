---
import Greeting from "../Greeting";

const { greeting_info, background_color, text_color } = Astro.props;
---

<div style={`background-color: ${background_color}; color: ${text_color};`}>
  <Greeting client:load greeting_info={greeting_info} />
</div>
